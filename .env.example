# SMTP settings (ex: Mailgun, Brevo, Gmail SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=mailer@example.com
SMTP_PASS=replace_me

# Contact routing
CONTACT_FROM=mailer@example.com
CONTACT_TO=hello@pixelflow.studio
CONTACT_TO_BACKUP=ops@pixelflow.studio

# Health check endpoint after deploy:
# GET /api/contact should return { ok: true }
